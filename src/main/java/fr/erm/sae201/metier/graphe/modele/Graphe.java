package fr.erm.sae201.metier.graphe.modele;

public class Graphe {
    
    private int[][] adjacenceMatrice;
    // private List<Secouriste> secoursite;
    // List<(Dps, Competence)> competenceDps; // liste des couples (DPS, compétence) qui sont dans le graphe, on peut avoir plusieurs fois la même compétence dans un DPS, mais pas dans le graphe, donc on a une liste de couple (DPS, compétence) et on les met dans le graphe.

    // utilise méthode findByDay du DAO (Journee)
    // Il faut matcher competence 

    // compétence figure petu etre plusieurs fois dans un DPS et plusieurs fois dans les journée inside du DPS, les deuxieme c'est un couple (compétence, DSP), on ilste tout et après les arrete qu'on a la c'est les possibiulité ''affectation.
    // Class graphe
    // juste cette classe la

    // Correspond juste a une journée précise
    // moins de secouriste que de compétence, on ajoute un secouriste fictif, mais a la fin il faut l'enlever.
    // et inverse
    // a la fin on supprime bien
}
